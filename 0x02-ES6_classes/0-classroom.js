//Represents a classroom

export default class ClassRoom {
// Accepts one parameter, maxStudent size and assigns it to a private field
  constructor(maxStudentsSize) {
// The value passed to the constructor is asigned to the private field. 
    this._maxStudentsSize = maxStudentsSize;
  }
}

